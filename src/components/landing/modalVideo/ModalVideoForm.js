import React from "react";
import {reduxForm} from "redux-form";

const ModalVideoForm = props => {
  return (
    <form>

    </form>
  );
};

export default reduxForm({form:"Add video"})(ModalVideoForm);